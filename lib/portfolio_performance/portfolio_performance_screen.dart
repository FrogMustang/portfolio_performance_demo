import 'package:flutter/material.dart';

class PortfolioPerformanceScreen extends StatelessWidget {
  const PortfolioPerformanceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Portfolio Performance'),
      ),
      body: Center(
        child: Text('Portfolio Performance'),
      ),
    );
  }
}
